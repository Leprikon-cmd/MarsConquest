# Mars LogBook - контекст проекта

Последнее обновление: 2026-09-09

Этот файл является главным рабочим документом проекта. Здесь собраны назначение приложения,
текущее состояние, устройство кода, принятые решения, активные задачи и основные вехи.

## Назначение продукта

Mars LogBook - локальный iPhone-компаньон и личный журнал партий в настольную игру
«Покорение Марса». Приложение помогает собрать состав партии, подсчитать результат,
сохранить подробности и наблюдать статистику игрока с течением времени.

Это не цифровая копия настольной игры. Основная ценность - принадлежащая пользователю история,
которая остаётся полноценной и без интернета. Подсчёт очков является важным инструментом журнала,
но не единственной целью приложения.

## Принципы продукта

- Сначала надёжная локальная история, затем необязательные сетевые возможности.
- Данные партии должны оставаться читаемыми после обновлений приложения и смены языка.
- Интерфейс может поддерживать атмосферу марсианской экспедиции, но не должен мешать обычным действиям.
- Публичный глобальный рейтинг без проверки результатов не является целью проекта.
- Будущие общие партии строятся вокруг закрытых групп друзей и подтверждения результата участниками.
- Турнирная статистика возможна отдельно, с ролью организатора или судьи.
- Карьерная система строится на скрытом `Career XP`: пользователь видит не числовой опыт,
  а репутационный статус, профессиональные титулы, карьерные события и историю своего пути.
- Основная лестница состоит из 19 статусов. Каждый статус в будущем получает отдельное
  графическое оформление; конкретные пороги и квалификационные требования балансируются отдельно.
- Целевой будущий образ главного экрана — развивающаяся коллекционная карта CEO, которая объединяет
  профиль, карьерный статус, специализации, статистику, регалии и историю владельца журнала.
- Любая партия, добавленная в Core Data штатным сохранением или импортом, является полноценной
  частью журнала и участвует в статистике, Career XP, карьерных событиях и наградах по всем реально
  сохранённым данным. Неполная старая запись не отбрасывается и не обесценивается: известные очки,
  озеленение, города, поле и другие параметры используются независимо от отсутствующих сведений.
- Отсутствующие в импортированной партии корпорация, пролог или другой параметр нельзя придумывать
  либо считать отрицательным результатом. Такая запись просто не участвует в конкретном условии,
  которое невозможно проверить по её данным.
- Career XP, репутационный статус, титулы и персональные события рассчитываются только для владельца
  текущего журнала. Участники его партий остаются частью истории и общей статистики, но приложение
  не ведёт за них отдельную карьеру без их собственного профиля владельца.
- Экспорт должен переносить полные записи партий со всеми сохранёнными участниками и показателями.
  После импорта на другом устройстве новый владелец вручную указывает, какой участник старой истории
  является им. После подтверждённой привязки приложение пересчитывает его статистику и карьеру по
  всем доступным данным этого участника во всех импортированных партиях.

## Стандарт комментариев в коде

Каждый Swift-файл начинается с краткой русской шапки: «Зачем», «Кто» и «Что можно менять руками».
Последний пункт обязан явно называть безопасные точки ручной настройки: отступы, размеры, шрифты,
цвета, тексты, изображения или правила. Если в файле нет таких настроек, это отмечается прямо.
Новые визуальные константы дополнительно сопровождаются коротким комментарием рядом с кодом,
если их назначение не очевидно по имени. Комментарии пишутся по-русски, кратко и описывают
причину решения, а не повторяют синтаксис Swift.

**EPIC-001 «Личный журнал игрока и новый главный экран»** реализован в базовом объёме.
Центральным объектом теперь является владелец журнала: он создаёт локальный профиль, автоматически
попадает в новые партии и видит личную статистику с недавними экспедициями. Этап «Надёжный журнал»
закрыт в согласованном объёме: технические долги, целостность сохранений, резервное копирование,
восстановление и проверка полного iPhone-сценария завершены. Все дальнейшие задачи карьерной системы
отложены в P8, в самый конец очереди. Нумерация прежних этапов сохранена для ссылок;
фактический порядок задаёт раздел «Очередь работы», первым выполняется P4.
Подробный замысел, границы реализованного и дальнейшая последовательность находятся в
`Documentation/concept.md`.

## Что работает сейчас

- создание партии с выбором игрового поля и дополнений;
- полноэкранный сбор команды на сменяющемся фоне;
- сохранённые профили игроков и защита от повторного добавления;
- уникальные имена игроков без учёта регистра и лишних пробелов;
- редактирование уже добавленного игрока;
- выбор цвета, корпорации, двух прологов и допустимого числа колоний;
- фильтрация корпораций по включённым дополнениям;
- замена недоступной корпорации на «Начинающую корпорацию» после отключения дополнения;
- ввод очков по категориям без отрицательных значений;
- оформление завершённой экспедиции как архивной записи: реквизиты без значений по умолчанию,
  последовательное уточнение поколения и, при включённой Венере, её шкалы от 0% до 30% с шагом 2%;
- достижения, награды, ничьи и итоговый рейтинг всех игроков;
- сохранение партии в Core Data одной операцией;
- защита кнопки сохранения от повторного быстрого нажатия;
- история партий, раскрываемая детализация и импорт встроенного `mars_import.json`;
  версионированный резервный архив создаётся и принимается через «Настройки», повторный импорт
  известного UUID не создаёт копию записи;
- статистика по играм, игрокам, корпорациям и прологам с нижним запасом под постоянную навигацию iPhone;
- подробности сохранённой экспедиции, полностью прокручиваемые выше нижней панели журнала;
- хранение и показ выбранных колоний;
- русская и английская локализация интерфейса и игрового справочника;
- ручной выбор языка или автоматический выбор по настройкам iPhone;
- необязательная фоновая дорожка МОКСИ;
- запуск и проверка основных сценариев на физическом iPhone.
- опубликованный публичный мини-сайт проекта с описанием, скриншотами, ссылкой на TestFlight
  и отдельной страницей политики конфиденциальности.
- профиль владельца журнала: обязательный ник, необязательное имя и настройки «Моя коллекция»;
- защита уникальности профилей по нормализованной паре «ник + имя»;
- первый запуск с созданием владельца или явным выбором себя из сохранённых игроков;
- автоматическое добавление владельца в новую экспедицию;
- стабильная привязка новых партий к `SavedPlayer` через UUID;
- ручная привязка импортированной старой истории по уникальному имени, без автоматического
  присвоения по совпадению текста;
- стартовый бортовой журнал владельца с партиями, победами, процентом побед, средним и лучшим
  результатом;
- единый фон бортового бейджа в виде светлой игровой карты сотрудника: чёрная полоса типа,
  отдельные места для уровня и карьерных меток, окно аватара, поле досье и нижняя карьерная фраза;
- персональный аватар владельца: пять встроенных изображений, выбор по нажатию на аватар в бейдже
  или через настройки, а также возможность сделать селфи фронтальной камерой;
- сделанное селфи кадрируется системным интерфейсом, хранится только локально в Application Support
  и сразу обновляется при повторной съёмке;
- последние пять экспедиций владельца с датой, полем, местом и итоговым счётом.
- совместимость с историческим импортом: старое написание поля `Элада` распознаётся как `Эллада`,
  поэтому статистика не делится на две группы.
- скрытый `Career XP` и уровень владельца: базовый опыт, места, игровые достижения и награды,
  первое поле, корпорация, пара «корпорация + пролог» и карьерные вехи;
- вкладка «Регалии», которая появляется только после подтверждённого повторяемого результата:
  карьерной вехи от 5 экспедиций или профессионального титула.

## Публичные страницы проекта

- Сайт: <https://leprikon-cmd.github.io/MarsConquest/>
- Политика конфиденциальности: <https://leprikon-cmd.github.io/MarsConquest/privacy.html>
- Публичный TestFlight: <https://testflight.apple.com/join/WnQq5pCD>
- Репозиторий: <https://github.com/Leprikon-cmd/MarsConquest>
- Контактная почта проекта: `log.book.mars@gmail.com`

Сайт публикуется через GitHub Pages из папки `docs` в ветке `main`. В ней находятся
`index.html`, `privacy.html`, общий `style.css`, favicon и три оптимизированных скриншота.
Корневые `README.md`, `LICENSE` и `PRIVACY_POLICY.md` используются как публичные документы
репозитория. Сайт не добавлен в Xcode-проект и не входит в сборку iOS-приложения.

## Главный сценарий

```text
MarsConquestApp -> OwnerProfileGateView -> ContentView (бортовой журнал)
                                                |
                                                v
                                     NewExpeditionView -> AddPlayersView
                                                                |
                                                                v
                                                          ScoreScreen
                                                                |
                                                                v
                                                           GameSaver
                                                                |
                                                                v
                                                             Core Data
                                                                |
                                                                v
                         RecentExpeditionsView / StatisticsScreen / GameDetailView
```

До внесения записи вся партия живёт в `LocalGameData`. `GameSaver` создаёт постоянные сущности Core Data
только после подтверждённого действия «Внести в журнал». Статистика и детали работают уже с сохранённой `Game`.

## Архитектура

1. **SwiftUI**: полноэкранные сценарии и переиспользуемые представления.
2. **Локальная модель**: `LocalGameData`, `LocalPlayer`, `LocalScore`, локальные награды и достижения.
3. **Менеджеры**: подсчёт, сохранение, импорт, настройки дополнений, звук и Core Data.
4. **Core Data**: партии, игроки, очки, бонусы, колонии, `SavedPlayer`, `OwnerProfile` и игровые справочники.

Визуальные настройки бейджа не требуют Core Data: выбранный аватар хранится через `AppStorage`.
Встроенные варианты описаны в `OwnerAvatarStyle`. Пользовательское селфи сохраняет
`OwnerSelfieStore`; файл остаётся внутри контейнера приложения и не отправляется во внешние сервисы.

Основные папки:

```text
Data/                 модели, справочники, Core Data и импортируемые типы
Documentation/        контекст, база, локализация и заметки релизов
Managers/             бизнес-логика, сохранение, импорт и сервисы
Resources/            звуки, карточки и встроенные данные
Screens/              полноэкранные пользовательские сценарии
Views/                переиспользуемые SwiftUI-компоненты
Assets.xcassets/      изображения и цвета приложения
MarsConquestApp.swift точка входа
```

Подробная схема сущностей и миграций находится в `DATABASE_SCHEMA.md`.

## Игровые данные и правила

`GameData.swift` содержит встроенный каталог. `InitialDataLoader` синхронизирует его с Core Data:
добавляет недостающие записи и удаляет точные дубликаты. Экраны выбора читают рабочие справочники
из Core Data.

`ScoreManager` складывает основные категории и бонусы:

- достижение: `+5` одному игроку, первым заявившему его;
- первое место в награде: `+5` каждому победителю при ничьей;
- при ничьей за первое второе место не распределяется;
- второе место: `+2`, только при трёх и более игроках и единственном первом месте;
- второе место может делиться между несколькими игроками, но не всеми участниками;
- политика учитывается только при включённом дополнении «Кризис»;
- места итогового рейтинга при ничьей идут плотно: `1, 1, 2, 3`.

## Карьерная система и Career XP

Карьерная система рассчитывается только для владельца текущего журнала. Career XP является
скрытым внутренним показателем: пользователь видит репутационный статус, титулы, события и развитие
карты CEO, но не числовой опыт. Сам по себе накопленный XP не даёт новый статус — одновременно
проверяются минимальное число партий и квалификационные условия соответствующей ступени.

### Опыт за участие и итоговое место

Базовый опыт за очередную завершённую партию постепенно уменьшается:

| Порядковый номер партии | Career XP |
|---:|---:|
| 1–50 | `+10` |
| 51–75 | `+5` |
| 76–100 | `+2` |
| 101 и далее | `+1` |

Регресс относится только к факту участия. Результат, достижения, награды, новые поля, корпорации,
прологи, титулы, рекорды и секретные события после сотой партии продолжают учитываться полностью.

В партии вдвоём первое место даёт дополнительно `+5 XP`, второе — `+2 XP`. При трёх и более
участниках первое место даёт `+5 XP`, второе — `+2 XP`, третье — `+1 XP`, остальные места не дают
дополнительного опыта. Базовый опыт получает каждый участник, однако карьерный расчёт выполняется
только для владельца журнала. При полной ничьей бонус места повторяет итоговое распределение мест.

В завершённой соло-партии неуспешная попытка даёт базовые `+10 XP`, успешная — суммарно `+15 XP`.
Для соло этот результат является итоговым начислением за участие и успех, а не `10 + 15`.

### Игровые достижения и награды

Опыт повторяет победные очки настольной игры:

- занятое игровое достижение: `+5 XP`;
- первое место в игровой награде: `+5 XP`;
- второе место в игровой награде: `+2 XP`.

При разделённой награде XP начисляется по тем же правилам, что и победные очки. Несколько занятых
достижений и наград складываются: это подтверждённый результат партии, а не случайный бонус.

### Освоение нового контента

- первая партия на конкретном игровом поле: `+50 XP`;
- первая партия конкретной корпорацией: `+10 XP`;
- первый опыт конкретной пары «корпорация + пролог»: `+5 XP`.

Два впервые использованных с этой корпорацией пролога дают `+10 XP`. Тот же пролог с другой
корпорацией считается новой карьерной комбинацией. Повтор уже встречавшейся пары бонуса не даёт.
Общего ограничения исследовательского бонуса за одну партию нет.

Отдельный исследовательский XP не начисляется за дополнение само по себе, новую колонию,
уникальную конфигурацию дополнений, первую победу корпорацией или первую победу на поле. Эти данные
остаются основой статистики, титулов, рекордов и секретных событий.

### Вехи количества партий

Каждая веха выдаётся один раз. При импорте старой истории все достигнутые вехи начисляются
ретроспективно.

| Веха | Career XP |
|---:|---:|
| 5 партий | `+10` |
| 10 партий | `+15` |
| 15 партий | `+20` |
| 20 партий | `+25` |
| 25 партий | `+30` |
| 50 партий | `+50` |
| 100 партий | `+100` |
| 150 партий | `+125` |
| 200 партий | `+150` |
| 250 партий | `+200` |

### Титулы, рекорды и секретные события

Профессиональные титулы являются скрытой веткой и не показываются заранее как список заданий.
Стартовые категории награды: первый профессиональный титул — `+25 XP`, развитый — `+50 XP`,
редкий — `+100 XP`, легендарный — `+150 XP`. Полученный титул может быть частью квалификации,
но его XP не отменяет минимальный стаж и остальные требования статуса.

Новый личный рекорд даёт `+10 XP`, только если улучшение действительно заметно. Для итогового счёта
и отрыва от второго места стартовый шаг составляет не менее `10 VP`; новая самая быстрая победа
также даёт `+10 XP`. Порог проверяется тестами и при необходимости меняется.

Секретные карьерные события награждаются по редкости: необычное — `+25 XP`, редкое — `+50 XP`,
очень редкое — `+100 XP`, легендарное — `+150 XP`. Каждое конкретное событие начисляется один раз,
если для него явно не предусмотрено несколько ступеней.

Новый репутационный статус отдельного XP не даёт. Наградой являются название, сообщение о
повышении, новый этап истории и автоматически изменившееся оформление карты CEO. Графическое
оформление не является отдельным расчётным событием.

### Итоговая формула и защита баланса

```text
Career XP =
  опыт за участие и итоговое место
  игровые достижения
  игровые награды
  освоение новых полей, корпораций и пар «корпорация + пролог»
  одноразовые вехи количества партий
  профессиональные титулы
  заметные личные рекорды
  секретные карьерные события
```

Жёсткий общий потолок XP за одну партию не вводится. От случайного перескакивания защищают
минимальное число партий и квалификация каждого статуса. После импорта или подтверждённой привязки
владельца Career XP пересчитывается из полной доступной истории в хронологическом порядке. Повторный
импорт той же партии не должен повторно увеличивать опыт.

### Реализованная граница первого расчёта

`CareerProgressCalculator` уже учитывает подтверждённые данными партии: регрессию базового опыта,
место, игровые достижения и награды, первое поле, первую корпорацию, пары «корпорация + пролог» и
вехи числа партий. Первое знакомство с полем, корпорацией или прологом даёт только скрытый XP;
личные регалии начинаются с подтверждённых повторяемых результатов — карьерных вех и титулов.
Расчёт идёт хронологически и учитывает только записи, явно связанные с владельцем по UUID;
совпадение имени не присваивает старую историю автоматически. Повтор одной и той же сохранённой
партии с тем же UUID не даёт XP второй раз.

Уже реализован первый профессиональный титул — «Человек корпорации» за 5 подтверждённых
экспедиций одной корпорацией. Он показывается отдельно от личных регалий и пока не добавляет XP:
полная модель титульных начислений будет введена вместе с остальными утверждёнными титулами.
Личные рекорды, секретные события и остальные квалификационные условия ещё требуют отдельных
правил и подтверждаемых данных. Поэтому уровень в бейджике сейчас повышается только при выполнении
уже доступных требований по числу партий и XP.
В соло-игре модель не хранит отдельный флаг успешного завершения: базовый опыт учитывается, но
бонус за успех не предполагается до появления такого подтверждённого поля.

### Тестовая шкала 19 статусов

| Статус | Минимум партий | Career XP |
|---:|---:|---:|
| 1 | 1 | 10 |
| 2 | 2 | 50 |
| 3 | 4 | 100 |
| 4 | 6 | 150 |
| 5 | 9 | 220 |
| 6 | 12 | 300 |
| 7 | 16 | 400 |
| 8 | 20 | 500 |
| 9 | 25 | 620 |
| 10 | 32 | 750 |
| 11 | 40 | 900 |
| 12 | 50 | 1050 |
| 13 | 65 | 1250 |
| 14 | 80 | 1450 |
| 15 | 100 | 1700 |
| 16 | 125 | 2000 |
| 17 | 150 | 2400 |
| 18 | 200 | 3200 |
| 19 | 250 | 4200 |

Это стартовая модель для реальных тестов, а не навсегда зафиксированный баланс. Ориентир: новый
игрок получает заметный импульс за освоение первых полей, корпораций и прологов; повторение
знакомого контента продолжает двигать карьеру, но постепенно медленнее; после сотой партии основной
рост дают результаты, титулы, редкие события и ещё не исследованные направления.

## Технические соглашения

- Навигация строится на `NavigationStack` и `navigationDestination`.
- Закрытие экранов выполняется через SwiftUI `dismiss()`.
- Каждое изменение Core Data получает новую версию модели; старые версии не удаляются.
- Новые пользовательские строки сразу добавляются в `Localizable.xcstrings` на русском и английском.
- Новые игровые сущности сразу получают постоянный ключ и локализованное отображение.
- Динамические строки используют активный `Locale` приложения, а не только язык iPhone.
- После каждого подтверждённого этапа обновляется соответствующая документация.
- Работа ведётся небольшими этапами: изменение, сборка, проверка пользователем, затем следующий шаг.
- Коммиты и отправку в удалённый репозиторий обычно выполняет владелец проекта.

Правила и ограничения локализации подробно описаны в `LOCALIZATION.md`, состав и порядок
целевых автоматических проверок — в `TESTING.md`.

## Активные задачи

### P0 - EPIC-001: базовый личный журнал

- [x] Профиль владельца опирается на `SavedPlayer`: обязательный ник, необязательное имя,
  настройки «Моя коллекция» и UUID-связь.
- [x] Уникальность задаёт нормализованная пара «ник + имя»; одинаковый ник допустим при разных именах.
- [x] Core Data расширена до `GameDataModel 8`: добавлены профиль владельца, поля ника и имени,
  а сохранённая запись игрока содержит `savedPlayerID`.
- [x] Первый запуск предлагает создать владельца или выбрать себя из существующих сохранённых игроков.
- [x] Владелец автоматически добавляется в новую экспедицию и может быть заменён в составе.
- [x] После «Высадка!» сразу открывается карточка владельца для выбора цвета, корпорации и прологов;
  экран команды не становится лишней промежуточной остановкой.
- [x] Новые партии связаны со стабильным UUID владельца; старая импортированная история привязывается
  только явным действием пользователя, сгруппированным по уникальному имени.
- [x] Главный экран владельца показывает базовую статистику и последние пять экспедиций.
- [х] Проверить отображение и переход в детали последних экспедиций на iPhone после очередной сборки.

### P1 - расширение личного журнала

- [x] Добавить игровые предпочтения и рекорды: частая корпорация, успешное поле, лучшая и быстрая победа.
- [x] Добавить среднее место, максимальное поколение и автоматический любимый цвет владельца.
- [x] Учесть историческую опечатку импорта `Элада`: при показе и подсчёте она объединяется с `Элладой`.
- [x] Отдельный экран удаления сохранённых игроков не нужен: профили сохраняют целостность истории.
  Исправление старых вариантов имени выполняется через «Проверить прошлые партии» — каждая группа
  записей вручную привязывается к одному UUID существующего профиля.
- [x] Отдельная карточка последней партии не нужна: текущий список последних экспедиций уже показывает
  дату, поле, место, итог, лидера и число участников, а полные данные открываются по нажатию.

### P2 - правила и стабильность текущего сценария

- [x] Проверен английский режим на маленьком и крупном iPhone: длина текста, даты, числа,
  пустые состояния и системные диалоги.
- [ ] Только после отдельного решения пользователя подготовить версию для iPad.
  До этого разработка и проверка ведутся только на iPhone; задача не входит в текущий этап.
- [x] Убрать автоматический выбор цвета, корпорации и прологов в обычном режиме: поля изначально
  пустые, добавление игрока недоступно до явного выбора. В настройках есть локальный «Тестовый
  режим», возвращающий автоподстановку для быстрого заполнения партий.
- [x] Реализовано последовательное определение победителя при равенстве итоговых очков: сначала
  сравниваются оставшиеся M€. Только если после этого равенство сохраняется, для этих игроков
  сравниваются неразыгранные карты; при полном равенстве сохраняется ничья.
  Старые партии без записанных данных тай-брейка не меняют результат.

### P3 - дизайн и удобство

- [x] Кнопки «Достижения» и «Награды» оформлены в стиле игрового поля.
- [x] Экран завершения экспедиции переработан из «подсчёта очков» в журнал: шапка документа,
  реквизиты, подтверждение достоверности сведений и спокойное сообщение о внесении в архив.
- [x] Подробности сохранённой игры и все разделы статистики получили нижний запас под постоянную
  навигацию iPhone; последняя строка больше не скрывается под панелью.
- [x] Бортовой бейдж оформлен как единая светлая игровая карта сотрудника с чёрным типом карты.
- [x] Добавлен выбор из пяти аватаров по нажатию на бейдж и локальное селфи с фронтальной камеры.
- Развитие карьерной карты CEO целиком перенесено в P8.
- [ ] Привести бортовой журнал, новую экспедицию, подсчёт и детали игры к единому спокойному стилю.
- [ ] Сделать победителя и итоговый рейтинг заметнее без перегрузки экрана.

### P4 - «Надёжный журнал»: завершён

- [x] После перехода на `GameDataModel 10` проверить миграцию на iPhone со старой базой:
  прежняя история открывается, новая экспедиция сохраняется и отображается штатно.
  Это подтверждённая пользователем проверка конкретной базы, а не всех вариантов миграции.

#### 1. Технические долги и сохранность данных

- [x] В `GameDataModel 11` исправить обратные связи `Game.colonies` и
  `Player.achievements`, сохранив модели 5–10 нетронутыми. Автоматический тест подтверждает
  взаимность активных связей и миграцию 10 → 11 с сохранением связанных объектов.
- [x] Пересмотреть глобальное подавление предупреждений компилятора модели. Оно временно сохранено,
  поскольку Xcode компилирует и неизменяемые исторические версии 5–10; причина документирована.
  Активная модель 11 проверяется отдельным тестом. При следующем изменении схемы решение пересмотреть.
- [x] Добавить автоматическую проверку сохранения полной партии и повторного чтения после сброса контекста:
  участники и UUID, категории очков, достижения, награды, колонии, поколение,
  шкала Венеры и тай-брейк совпадают с введёнными данными. Перезапуск приложения на физическом
  iPhone подтверждён пользователем как штатный сценарий.
- [x] Изолировать `GameSaver` от основного контекста: сохранение партии больше не записывает
  и не откатывает посторонние несохранённые изменения; удалён общий `rollback` из `ScoreScreen`.
- [x] Искусственно вызвать ошибку сохранения и проверить повторную попытку: в тесте после отказа
  в архив не попадает частичная партия, а повторное внесение с тем же UUID оставляет одну запись.
  Встроенный исторический импорт `mars_import.json` также выполняется в отдельном контексте и
  при ошибке не оставляет частичных записей в основном журнале. Новые исторические импорты также
  сохраняют номер партии и постоянные ключи игрового справочника, чтобы смена языка не ухудшала историю.
- [x] Заменить `fatalError` при невозможности открыть хранилище на отдельный экран:
  исходная база не удаляется и не заменяется, приложение сообщает о временной недоступности
  архива и позволяет раскрыть техническую причину. Искусственное воспроизведение ошибки
  на iPhone остаётся частью итоговой проверки этапа.
- [x] Проверить миграции на сохранённых тестовых базах поддерживаемых версий. Переход 10 → 11
  уже покрыт автоматическим тестом на временной SQLite-базе.
  Автоматический контур также создаёт минимальные SQLite-базы моделей 5–9 и проверяет их
  переход к `GameDataModel 11`; реальные пользовательские базы и полный состав данных каждой
  исторической версии остаются отдельной проверкой.
  Сейчас включены автоматическая миграция и вывод mapping model; отдельного механизма
  последовательного перехода по всем версиям в `CoreDataManager` нет. Если появится реальная
  база версий 5–9, её открытие остаётся полезной пострелизной контрольной процедурой.

#### 2. Резервная копия и восстановление

- [x] Реализовать версионированный экспорт и импорт из пользовательского файла: полные партии,
  участники, стабильные идентификаторы, все сохранённые игровые показатели и связи.
- [x] Определить и реализовать состав резервной копии профиля: владелец, сохранённые игроки,
  «Моя коллекция» и локальный аватар с селфи. Язык, звук и тестовый режим остаются настройками
  конкретного iPhone; состав прямо указан в настройках перед созданием архива.
- [x] Защитить повторный импорт от дублирования и определить обработку конфликтующих записей:
  сущности с уже существующим UUID не перезаписываются и не создаются повторно. Это правило
  относится к новому архиву журнала; старый встроенный `mars_import.json` по-прежнему является
  отдельным разовым импортом исторических данных.
- [x] Проверять файл до внесения изменений; неподдерживаемый, повреждённый или внутренне
  несогласованный архив отклоняется до записи в Core Data, а частный контекст откатывается
  при любой ошибке восстановления.
- [x] При переносе чужой истории после принятия архива предложить владельцу явно указать себя
  среди участников реально добавленных партий. Связь выполняется только по выбранному постоянному
  UUID профиля, никогда — по совпадению имени; отказ от выбора оставляет историю общей.
- [x] Проверить вручную экспорт и повторный импорт архива: пользователь подтвердил, что архив
  создаётся, а повторное принятие сообщает о уже имеющихся партиях и не создаёт копий. Перенос
  на отдельный чистый iPhone остаётся рекомендуемой пострелизной контрольной процедурой.

#### 3. Проверка полного сценария и интерфейсная устойчивость

- [x] Разделить быстрые проверки по целевым наборам: правила подсчёта, состав экспедиции,
  совместимость старых записей, сохранение/миграция и UI smoke. UI-тесты используют отдельную
  чистую базу в памяти; карта запусков описана в `Documentation/TESTING.md`.
- [x] Актуализировать UI-тест после «Высадка!»: тест подтверждает открытие редактора владельца,
  возврат к стандартной работе с командой и доступность нижних кнопок на iPhone.
- [x] Пройти полный сценарий на iPhone в RU/EN: владелец → команда → очки → подтверждение
  записи → детали → повторное открытие и сверка результатов. Пользователь подтвердил штатную
  работу; по итогам устранена и локализована подпись первого подтверждения участия владельца.
- [x] Подготовить русскоязычный маршрут до деталей внесённой записи: сценарий изолирован от
  пользовательской базы. Прогон в UI runner не является блокером: среда симулятора на текущем
  Mac нестабильна, а основной маршрут подтверждён на iPhone.
- [x] Проверить отсутствие runtime-предупреждений размеров SwiftUI при основных переходах:
  после защиты вычисляемых размеров и ручной проверки пользователя предупреждения не воспроизводятся.
- [x] Заменить повторяющиеся отступы `84` единым правилом `JournalNavigationLayout` для размещения
  содержимого над постоянной навигацией iPhone.
- [x] Вручную проверить маленький и крупный iPhone, клавиатуру и увеличенный размер текста на
  длинных списках журнала, статистики и подробностей экспедиции. Пользователь подтвердил:
  последняя строка и кнопки остаются доступны выше навигации и клавиатуры.

**Критерий завершения:** выполнен. Старая история читается после обновления; новая партия полностью
сохраняется и открывается после перезапуска; резервная копия восстанавливается без потерь;
повторный импорт не создаёт дубликатов; ошибки не разрушают существующий журнал;
основной сценарий RU/EN подтверждён проверками. Редкие проверки отдельных старых баз и переноса
на запасное устройство вынесены в пострелизный контроль и не блокируют расширение функций.

### P5 - компаньон партии

- [ ] Добавить переключатель «Драфт» и сохранять его вместе с партией.
- [ ] Добавить необязательный режим первого игрока, порядка хода, передачи телефона,
  «Пасса» и окончания поколения.
- [ ] Добавить мягкий общий таймер и журнал длительности поколений.
- [ ] Добавить шахматный режим с личным временем игроков.
- [ ] Продумать домашние правила при превышении времени.
- [ ] Сохранять время, драфт и режим контроля времени в статистике.
- [x] Добавить ввод и сохранение итоговой шкалы терраформирования Венеры как необязательной
  характеристики партии: пикер от 0% до 30% с шагом 2%, хранение в Core Data и показ в деталях игры.
  Персональная статистика и события по Венере остаются отдельным будущим шагом.

### P6 - закрытые группы

- Полный экспорт, восстановление и защита от повторного импорта перенесены в P4.
- [ ] Создать закрытые группы друзей и приглашения.
- [ ] Реализовать общую партию с вводом и подтверждением результатов участниками.
- [ ] Записывать подтверждённую партию в локальную историю каждого участника.
- [ ] Продумать исправление и повторное подтверждение результата.
- [ ] Отмечать официальные и домашние правила / партии вне рейтинга.

### P7 - клубы и турниры

- [ ] Добавить роль организатора или судьи.
- [ ] Фиксировать официальный состав, дополнения и правила турнира.
- [ ] Вести отдельную подтверждённую турнирную статистику.
- [ ] Добавить журнал подтверждений и исправлений.

### P8 - карьера: самый конец очереди, работа приостановлена

Существующие возможности сохраняются. Новые статусы, XP, регалии, титулы, рекорды,
карьерные события и развитие карты CEO не входят в текущие работы.

- [ ] Преобразовать главный экран в коллекционную карту CEO: портрет, статус, титул,
  специализации, статистика, регалии и описание карьеры.
- [ ] Подготовить визуальное развитие карты CEO для разных ступеней репутации.
- [x] Зафиксировать структуру основной лестницы из 19 репутационных статусов и её рабочие названия.
- [x] Реализовать первый слой `CareerProgressCalculator`, вкладку «Регалии» для повторяемых
  результатов и титул «Человек корпорации» за 5 партий одной корпорацией.
- [ ] Оформить все 19 статусов графически как последовательное развитие карьеры CEO.
- [ ] Довести Career XP до полной модели: титулы, личные рекорды, секретные события, их
  квалификационные условия и подтверждённый результат соло-партии. Для титулов сначала утвердить
  условия универсала, колонизатора, экологического и быстрого/долгого стратега; для рекордов —
  лучший счёт, заметный отрыв и быструю победу.
- [ ] При расчёте карьеры одинаково учитывать штатно сохранённые и импортированные партии.
  Каждый показатель старой неполной записи использовать независимо; условия по отсутствующим
  полям не выполнять и не считать проваленными.
- [ ] Показать текущий статус и ближайшее условие роста в бортовом бейджике и отдельном экране карьеры.
- [ ] На основе истории определять часто используемые, редкие и наиболее успешные колонии, а также
  связанные события и титулы.
- [ ] Отслеживать интересные редкие комбинации «корпорация + пролог».
- [ ] Использовать активные игровые достижения и награды в статистике и условиях подтверждаемых
  карьерных событий, не смешивая их с личными регалиями Mars LogBook.
- [ ] Подготовить следующие персональные достижения Mars LogBook и историю карьерных событий.
- [ ] Проверить баланс скрытого Career XP на реальной истории партий до закрепления окончательных
  значений начислений и порогов.

## Очередь работы

1. P4 «Надёжный журнал»: диагностика технических долгов, сохранение, связи и миграции базы.
2. P4: резервная копия, восстановление, защита от повторного импорта.
3. P4: полный сценарий RU/EN на iPhone, проверка ошибок и устойчивости интерфейса.
4. P3: единый спокойный стиль экранов, заметность победителя и итогового рейтинга.
5. P5: компаньон партии — драфт, порядок хода, таймер и журнал поколений; дальнейшая Венера
   остаётся отложенной до этого этапа.
6. P6–P7: закрытые группы и турнирный режим. iPad — только по отдельному решению пользователя.
7. P8: все дальнейшие работы по карьере — в самом конце очереди.

## Известные ограничения

- Некоторые карточки прологов и колоний содержат русский текст в английском режиме.
- `error.localizedDescription` может следовать системному языку iPhone.
- Старая запись без постоянного ключа показывает исходное сохранённое название.
- Перед добавлением третьего языка нужно убрать оставшиеся ручные ветвления `русский/английский`.
- Источник и условия использования части игровых изображений должны оставаться документированными.

## Ключевые выполненные этапы

- Core Data доведена до `GameDataModel 11`, включены автоматическая миграция и автоматический
  вывод модели сопоставления. Переход со старой базы подтверждён пользователем на iPhone;
  переход 10 → 11 также проверен автоматически; полная проверка поддерживаемых версий запланирована в P4.
- Локальная модель партии отделена от постоянного хранилища.
- Награды, достижения, колонии, шкала терраформирования Венеры и полная разбивка очков сохраняются
  и открываются из истории.
- Реализованы корректные ничьи наград и плотная нумерация итогового рейтинга.
- Экран команды стал полноэкранным, игроки редактируются, карточки корпораций и прологов видны в составе.
- Загрузчик справочников стал повторяемым и не создаёт дубликаты.
- Интерфейс и игровой справочник локализованы на русский и английский языки.
- Игровые поля, прологи, колонии, награды и достижения получили постоянные идентификаторы.
- Основной регрессионный сценарий пройден на симуляторе и физическом iPhone.
- Создан и опубликован официальный мини-сайт Mars LogBook на GitHub Pages.
- Реализован базовый личный журнал: профиль владельца, первый запуск, автоматическое добавление в новую партию, UUID-связь и недавние экспедиции.
